#include<stdio.h>
#include<conio.h>
int main()
{
printf("the size of int is %u bytes",sizeof(int));
printf("the size of float is %u bytes is ",sizeof(float));
printf("the size of double is %u bytes is",sizeof(double));
printf("the size of char is %u byte is",sizeof(char));
return 0;
}


